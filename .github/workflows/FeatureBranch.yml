name: GitHub Actions for Feature Branches

on:
  push:
    branches:
      - 'feature/*'

jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Running Step 1"
      - name: Step 2
        run: echo "Running Step 2"
      # Add more steps as needed for your workflow
